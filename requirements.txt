# ============================================================
# RACEJUDGE — Python dependencies
# Phase 1 (scraping + parsing) — minimal set to get started
# ============================================================

# HTTP + scraping
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.2.0          # faster BS4 HTML parser
playwright>=1.60.0   # headless browser for FIA season filter (JS-rendered)

# PDF parsing
pdfplumber>=0.11.0

# Data
orjson>=3.10.0       # fast JSON
python-dotenv>=1.0.0

# Logging / CLI
rich>=13.7.0         # pretty terminal output
tqdm>=4.66.0         # progress bars

# Dev
ruff>=0.4.0
mypy>=1.10.0
pytest>=8.2.0
pytest-cov>=5.0.0


# ============================================================
# Phase 2 additions (uncomment when starting Phase 2)
# ============================================================
# transformers>=4.41.0
# torch>=2.3.0
# pillow>=10.3.0
pytesseract>=0.3.10              # OCR fallback for scanned PDFs
pdf2image>=1.17.0               # render PDF pages for Tesseract
# layoutparser>=0.3.4


# ============================================================
# Phase 3 additions (uncomment when starting Phase 3)
# ============================================================
# faster-whisper>=1.0.0          # Whisper ASR (CTranslate2 backend)
# pyannote.audio>=3.1.0          # speaker diarization (requires HF_TOKEN)
fastf1>=3.3.0                    # F1 telemetry (needed for TelemetrySlicer)
celery[redis]>=5.4.0             # async task queue (parse_pdf, extract_text, ocr_fallback workers)
redis>=5.0.0                     # Redis client (Celery broker + result backend)
flower>=2.0.0                    # Celery task monitor UI (optional)
prefect>=3.0.0                   # pipeline orchestration


# ============================================================
# Phase 4 (active)
# ============================================================
sentence-transformers>=3.0.0     # BGE-M3 embeddings
pgvector>=0.3.0                  # pgvector Python client
asyncpg>=0.29.0                  # async Postgres driver
sqlalchemy[asyncio]>=2.0.0       # ORM + async support
alembic>=1.13.0                  # DB migrations


# ============================================================
# Phase 5 (active)
# ============================================================
xgboost>=2.0.0                   # penalty prediction model Layer A
scikit-learn>=1.5.0              # preprocessing + meta-learner
joblib>=1.4.0                    # model serialization
# peft>=0.11.0                   # LoRA fine-tuning for Llama Layer B (deferred — GPU required)
anthropic>=0.40.0                # RAG explainer (claude-haiku)


# ============================================================
# Phase 1 / API skeleton (active — needed for FastAPI dev server)
# ============================================================
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0                    # required by FastAPI TestClient + live polling


# ============================================================
# Phase 7 (active)
# ============================================================
stripe>=10.0.0                   # Stripe billing + webhook verification
psycopg2-binary>=2.9.9           # sync psycopg2 for pipeline scripts


# ============================================================
# Phase 8 (active)
# ============================================================
sentry-sdk[fastapi]>=2.0.0       # error monitoring + performance tracing
