# ============================================================
# RACEJUDGE API runtime — slim dependency set for the Fly container.
#
# Deliberately EXCLUDES the heavy ML / browser / orchestration libs
# (torch, sentence-transformers, fastf1, playwright, pdfplumber, xgboost,
# scikit-learn, celery, prefect). Those run in separate worker / training
# jobs — never in the request-serving API process. The few endpoints that
# need them (predict [gated off], /incidents/extract, telemetry) lazy-import
# at call time, so the API boots clean without them.
#
# Full dev/worker deps remain in requirements.txt.
# ============================================================

# Web
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0
orjson>=3.10.0
python-dotenv>=1.0.0

# Database + migrations
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pgvector>=0.3.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Cache + live pub/sub
redis>=5.0.0

# Auth + billing
PyJWT[crypto]>=2.8.0
stripe>=10.0.0

# RAG explainer (lazy-loaded by /v1/predict/explain)
anthropic>=0.40.0

# Observability
sentry-sdk[fastapi]>=2.0.0
